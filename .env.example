# Trakt API Configuration
# Get your credentials from: https://trakt.tv/oauth/applications
TRAKT_CLIENT_ID=your_trakt_client_id_here
# Note: TRAKT_CLIENT_SECRET is only used in the serverless function, not exposed to frontend

# MyAnimeList API Configuration
# Get your credentials from: https://myanimelist.net/apiconfig
MAL_CLIENT_ID=your_mal_client_id_here
# Note: MAL uses PKCE flow, so no client secret is needed

# Application Configuration
APP_URL=http://localhost:4200

# Optional: For production deployment
# APP_URL=https://your-production-domain.com
